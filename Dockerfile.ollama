FROM ubuntu:22.04

RUN apt-get update && apt-get install -y curl

# Install Ollama
RUN curl -fsSL https://ollama.com/install.sh | sh

# Pull small model during build
RUN ollama serve & sleep 5 && ollama pull phi

EXPOSE 11434

CMD ["ollama", "serve"]